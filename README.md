## Тестовое задание для Enot

Пара комментариев по реализации:

- Я не был уверен на сколько конкретная реализация нужна, поэтому описал в коде все, кроме представлений.
- Из-за большого количества времени, которое было потрачено на это задание, не успел грамотно реализовать работу с 
проверкой подтверждения одноразового кода. Сделано достаточно топорно прям в методах контроллера. 
Стоило бы эту логику вынести в отдельный middleware по аналогии со стандартным password.confirm или контроллер, 
который наследуется от базового и который должны будут наследовать контроллеры, методы которых нуждаются в подобной защите

### P.S.

Надеюсь в этом примере достаточно кода, дабы оценить мой уровень.
Если необходимо, я готов ещё посидеть и вынести логику верификации одноразовым кодом в отдельный слой.
В любом случае интересно будет получить фидбек по текущей реализации.
